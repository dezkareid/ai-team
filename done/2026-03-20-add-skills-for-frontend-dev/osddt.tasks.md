# Tasks: Frontend Development Skills

## Phase 1: Skill Acquisition
- [x] [S] Import `react-best-practices` from `vercel-labs/agent-skills` using `npx skills`.
- [x] [S] Import `next-best-practices` from `vercel-labs/next-skills` using `npx skills`.
- [x] [S] Verify the presence of new skill files in the `skills/` directory.

### Definition of Done (Phase 1)
- The skills `react-best-practices` and `next-best-practices` are successfully imported into the `skills/` directory.

## Phase 2: Configuration Integration
- [x] [S] Register the `frontend-tools` plugin in `.agent-structurerc`.
- [x] [S] Map `react-best-practices` skill to the `frontend-tools` plugin in `.agent-structurerc`.
- [x] [S] Map `next-best-practices` skill to the `frontend-tools` plugin in `.agent-structurerc`.

### Definition of Done (Phase 2)
- `.agent-structurerc` is updated and correctly represents the new plugin and its skills.

## Phase 3: Cleanup and Export
- [x] [S] Delete any `.claude/` directories or Claude-specific configuration files generated by `npx skills`.
- [x] [S] Run `pnpm run build` to prepare the project's internal tools.
- [x] [M] Run `pnpm run export-claude` and verify the `plugins/frontend-tools/` directory is created.
- [x] [S] Run `pnpm run distribute-mcp` to update the Gemini extension metadata.

### Definition of Done (Phase 3)
- Redundant files are removed and all distribution artifacts (Claude plugins and Gemini extension) are successfully generated and consistent with the new skills.

## Dependencies
- Phase 1 must complete before Phase 2 (to ensure skill files exist).
- Phase 2 must complete before Phase 3 (to ensure the export script has the correct mapping).
